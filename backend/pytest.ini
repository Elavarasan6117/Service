[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -q --strict-markers
markers =
    postgis: requires a real PostgreSQL+PostGIS database (set TEST_POSTGRES_URL)
filterwarnings =
    ignore::DeprecationWarning
